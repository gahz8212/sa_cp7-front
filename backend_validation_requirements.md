# 엑셀 업로드 데이터 검증(Validation) 백엔드 작업 명세서

프론트엔드에서 거대한 전체 데이터를 전송하는 방식에서, 매핑 정보만 전송하고 실제 데이터 검증은 백엔드에서 처리하도록 변경되었습니다. 이에 따라 백엔드에 구현해야 할 로직을 정리합니다.

## 1. API 엔드포인트
- **URL**: `/api/common/validate-excel`
- **Method**: `POST`

## 2. Request Body (프론트엔드 -> 백엔드)
프론트엔드에서는 임시 보관된 엑셀 파일의 `file_key`와 사용자가 선택한 컬럼의 매핑 정보(`columnMappings`)만을 전송합니다.

```json
{
  "file_key": "a1b2c3d4-e5f6...",
  "columnMappings": [
    {
      "col-index": 2,            // 매핑된 엑셀의 열(Column) 인덱스 (0-based)
      "relative-row": 0,         // 다중 행 레코드일 경우 상대적인 행 위치 (보통 0)
      "back-column": "userName"  // 백엔드(시스템)에서 사용하는 컬럼명/식별자
    },
    {
      "col-index": 4,
      "relative-row": 0,
      "back-column": "userAge"
    }
  ]
}
```

## 3. 백엔드 처리 로직 (Action Items)

### Step 1. 엑셀 파일 조회
- 전달받은 `file_key`를 사용하여 서버(또는 캐시/스토리지)에 임시 저장되어 있는 원본 엑셀 파일을 조회하고 읽어옵니다.

### Step 2. 데이터 추출 및 매핑
- 조회한 엑셀 파일의 실제 데이터 영역(Row)들을 순회합니다.
- `columnMappings` 배열을 참조하여, 각 행에서 `col-index` 위치에 있는 값을 읽어옵니다.
- 읽어온 값을 `back-column` 이름에 맞게 객체(Map) 형태로 재구성합니다.
  - *예시: `{ "userName": "홍길동", "userAge": 30 }`*

### Step 3. 데이터 유효성 검증 (Validation)
- 재구성된 데이터 목록을 대상으로 실제 검증 로직을 수행합니다.
- **주요 검증 항목 예시**:
  - **필수 값 체크**: 누락된 값이 없는지 (NotNull, NotEmpty)
  - **데이터 타입 체크**: 숫자형 컬럼에 문자가 들어오지 않았는지
  - **정규식(Regex) 체크**: 이메일, 전화번호 형식 등이 맞는지
  - **비즈니스 로직 검증**: 이미 데이터베이스에 존재하는 중복 데이터인지 등

### Step 4. 검증 결과 응답 (Response)
- 검증에 성공했다면 성공 응답(`200 OK`)을 내려줍니다.
- 만약 유효하지 않은 데이터가 발견되었다면, 프론트엔드가 사용자에게 오류 위치와 사유를 명확히 보여줄 수 있도록 상세한 에러 데이터를 반환합니다.

#### 에러 응답 포맷 예시 (권장)
```json
{
  "success": false,
  "message": "데이터 검증에 실패했습니다.",
  "errors": [
    {
      "rowIndex": 12,                  // 에러가 발생한 엑셀의 원본 행 번호
      "columnCode": "userAge",         // 에러가 발생한 back-column 명
      "errorMessage": "나이는 숫자만 입력 가능합니다.",
      "invalidValue": "서른살"         // 실제로 잘못 입력된 값
    }
  ]
}
```

## 4. 참고 사항
- 엑셀 파일을 읽어올 때 데이터가 시작되는 "데이터 행(Data Row)"의 시작점을 백엔드에서 어떻게 판단할지 논의가 필요할 수 있습니다. (필요 시 프론트엔드에서 `dataStartRow` 값을 함께 넘겨주도록 추가 수정이 가능합니다.)
- 검증이 완료된 이후, 사용자가 최종 제출을 누르면 이 검증 로직을 거친 데이터를 실제 DB에 저장하는 `/api/common/save-excel-data` 같은 엔드포인트가 이어서 필요합니다.
