# 엑셀 헤더/데이터 자동 탐지 로직 (결제선 노이즈 필터링) 진행 상황 메모

## 1. 현재 달성한 사항 (알고리즘 정상화)
사용자께서 공유해주신 브라우저 로그를 통해 **알고리즘 자체는 완벽하게 의도대로 동작하고 있음**을 확인했습니다.

*   **결제선 우측 편향 기준 완화**: 
    `firstNonEmpty >= Math.floor(totalCols / 3)` 조건으로 업데이트하여 `[빈칸, 문자, 문자, 문자]`나 `[빈칸, 빈칸, 문자, 문자, 문자]` 같은 형태의 실무형 결제선을 성공적으로 노이즈(`isNoiseRow`)로 판별하도록 수정했습니다.
*   **메타데이터 오염 해결**: 
    `rowToValues` 함수에서 내부 주입 메타데이터(`rowIndex`, `_originalRowIndex`)를 제외시켜, 알고리즘이 빈 행을 정확히 빈 행으로 인식하고, 열의 개수(`totalCols`)를 부풀리지 않도록 근본적인 버그를 해결했습니다.
*   **로그 검증 완료**:
    ```text
    Row 0: ['empty', 'empty', 'string', 'string', 'string'] 
    scanStart index after noise skipping: 1 
    // 1행(Row 0)을 완벽하게 노이즈로 스킵 완료

    [detectDataArea] Final result -> dataStartRow: 3, recordHeight: 2
    // 4행, 5행을 다단 헤더로 인식하고, 6행(Row 3)부터 2단 데이터로 완벽하게 탐지 완료
    ```

## 2. 발생했던 문제와 UI 처리 로직 수정 내역
알고리즘이 1행을 버렸음에도 화면(UI)에서는 여전히 1행이 파란색(헤더)으로 칠해지는 문제가 있었습니다.
*   **수정 1**: `detectDataArea`가 `headerStartRow` (결제선을 건너뛴 실제 헤더 시작점)를 반환하도록 수정했습니다.
*   **수정 2**: 저장된 템플릿(과거 업로드 기록)이 없을 때 UI를 그리는 기준(`hBaseRow`)을 `0`이 아닌 `headerStartRow`를 사용하도록 수정했습니다.
*   **수정 3**: 백엔드에 과거 저장된 템플릿 정보(`savedHeaderStructure`)가 있어서 이를 불러올 때, 알고리즘 분석 결과가 더 정확하다면 `sBaseRow`뿐만 아니라 **`hBaseRow`도 함께 덮어씌우도록(`hBaseRow = autoDetected.headerStartRow`) 수정**했습니다.

## 3. 남은 미스터리 (다음 세션 디버깅 포인트)
모든 백엔드 템플릿 오버라이딩과 UI 로직을 수정했음에도 여전히 화면에서 결제선이 헤더로 잡혀 나오는(또는 화면 변화가 없는) 상황입니다. 다음 세션에서는 아래 항목들을 최우선으로 점검해야 합니다.

1.  **UI 컴포넌트의 렌더링 범위 의심**: 
    `useExcelStore.ts`의 로직은 완벽해졌으나, `VirtualGrid`나 테이블 컴포넌트에서 `allData`를 렌더링할 때 `selectedHeaderRows`의 Set 데이터를 무시하고 강제로 인덱스 0부터 렌더링하는 숨겨진 코드가 있는지 확인이 필요합니다.
2.  **FilterRows 함수의 사각지대**: 
    `filterRows` 함수 내에서 `헤더 영역에서는 노이즈(우측편향) 삭제 방어` 로직이 있는데, 이로 인해 `newRows` 배열에 1행(결제선)이 삭제되지 않고 물리적으로 남아버립니다. `selectedHeaderRows`에서 제외되어 회색(Etc)으로 변하더라도 눈에 보이는 것 자체가 불만이실 수 있으므로, 아예 `filterRows`에서 결제선을 원천 삭제해버리는 방향으로 수정할지 논의가 필요합니다.
3.  **Next.js 및 브라우저 캐시 문제**: 
    로컬 스토어 밖에서 상태가 캐싱되어 `handleUpload` 로직이 낡은 코드로 동작하고 있을 가능성을 완전히 배제하기 위해, 터미널 재부팅 및 강력 새로고침 환경에서 다시 테스트해 볼 필요가 있습니다.
