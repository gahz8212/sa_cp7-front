# 엑셀 업로드 구조 해석 및 검증 로직 정리

본 문서는 `apps/admin/app/test/excel-upload/page.tsx`에 구현된 엑셀 구조 해석 로직의 핵심 원리와 구조를 정리합니다.

## 1. 주요 기능 및 성능 최적화

- **영역별 행 위치 관리**: '헤더', '데이터', '기타' 영역별로 독립적인 `useRef`(`headerBaseRowRef`, `sampleBaseRowRef`, `etcBaseRowRef`)를 사용하여, 데이터가 시작되는 실제 엑셀 행 번호를 정확히 추적합니다.
- **성능 최적화 (대용량 데이터 대응)**:
  - **O(1) 검색 (Map/Set)**: `mappingResult` 스키마 검색 시 `Map`을 사용하고, 선택 상태 관리에 `Set`을 도입하여 대용량 데이터(1000개 이상)에서도 렌더링 지연을 최소화했습니다.
  - **가상화 효과 (Paging)**: `pageSize` 기반 페이징과 검색 최적화를 결합하여 `Input` 컴포넌트의 부하를 관리합니다.
  - **안전한 데이터 복사**: `structuredClone` 대신 안전하고 빠른 얕은 복사(`map`)를 사용하여 브라우저 호환성과 속도를 동시에 확보했습니다.
- **진행률 표시 (Progress Bar)**:
  - **Axios Hook 연동**: `onUploadProgress`를 통해 서버 전송 상태(0~50%)를 실시간으로 표시합니다.
  - **가공 진행률**: 서버 수신 후 프론트엔드 데이터 정규화 과정을 단계별(50~100%)로 시각화하여 사용자 경험을 개선했습니다.
- **병합(rowspan/colspan) 처리**: `getStructuredData` 및 `getStructuredType` 함수에서 빈 셀을 확인하여 엑셀의 병합 구조를 계산하고, 최종 데이터에 `rowspan`과 `colspan` 속성을 포함합니다.

## 1. 주요 기능 (업데이트)

- **영역별 행 위치 관리**: '헤더', '데이터', '기타' 영역별로 독립적인 `useRef`(`headerBaseRowRef`, `sampleBaseRowRef`, `etcBaseRowRef`)를 사용하여, 데이터가 시작되는 실제 엑셀 행 번호를 정확히 추적합니다.
- **헤더 세부 선택 로직**:
  - **행 전체 선택**: 행 번호(#) 클릭 시 행 전체가 노란색(`bg-yellow-200`)으로 선택됩니다.
  - **개별 셀 토글**: 선택된 행 내의 셀을 클릭하면 흰색(`bg-white`)으로 토글되며, 이 상태가 '선택된 헤더'로 간주됩니다.
- **상태 관리 최적화**:
  - `selectedHeaderCells`(좌표)를 원천 데이터로 삼고, `selectedHeaders`(값 목록)는 `useMemo`를 통해 파생하여 관리함으로써 데이터 정합성을 유지합니다.
  - 사용하지 않는 `modifiedData` 상태를 제거하여 로직을 단순화했습니다.
- **구조 해석 필터링**: 선택된 헤더 값(`selectedHeaders`)과 일치하는 항목을 구조 해석 결과(`flattenedHeaders`)에서 자동으로 제외하여 필요 없는 컬럼을 수동으로 필터링할 수 있습니다.
- **스키마 Row 값 통일**: 변환된 구조 타입 데이터의 `row` 값을 `sampleBaseRowRef.current`로 통일하여 데이터 매핑의 기준점을 명확히 했습니다.
- **1:1 데이터 추출**: 복잡한 병합(rowspan/colspan) 계산 로직을 제거하고, 엑셀 원본의 1x1 셀 구조를 그대로 보존하는 단순화된 추출 방식을 채택했습니다.
- **정확한 좌표 계산**: 데이터 추출 함수(`getStructuredData`, `getStructuredType`)는 `startRow` 파라미터를 사용하여, 결과 데이터의 `row` 속성에 실제 엑셀 시트상의 행 번호(`r + startRow`)를 정확히 반영합니다.
- **데이터 타입 검증**: 추출된 데이터를 기반으로 `number`, `phone`, `biz-number` 등의 타입을 추론하고, 타입 불일치 시 UI상에서 셀을 적색 테두리(`border-red-500`)로 표시합니다.

## 2. 핵심 로직 구조

- `handleRowClick`: 각 모드(HEADER, DATA, ETC)별로 행 선택 시 해당 영역의 `BaseRowRef`를 선택된 행 중 가장 첫 번째 행 번호로 업데이트합니다.
- `getStructuredData` / `getStructuredType`: 엑셀 매트릭스를 순회하며 실제 데이터를 추출하고 병합 구조(`rowspan`, `colspan`)를 계산합니다.
- `mergeHeaderAndType`: 헤더 컬럼 정보와 타입 정보(병합 정보 포함)를 결합하여 최종 스키마를 생성합니다.
- `renderTable`: 페이지 이동 시에도 `mappingResult`의 스키마를 바탕으로 모든 셀의 유효성을 실시간으로 검사합니다.

## 3. UI 구성

- **구조 해석 결과**: `renderMappingTable` 함수를 통해 해석된 헤더 및 데이터 영역의 병합 구조를 시각적으로 미리 확인할 수 있습니다. 이때 원본 행렬(Matrix) 데이터를 사용하여 정확한 테이블 레이아웃을 보장합니다.

## 4. 주의사항

- 헤더, 데이터, 기타 영역 선택 시, 각 데이터가 시작되는 행을 정확히 선택해야 정확한 좌표(`row`)가 계산됩니다.
## 5. 리팩토링 및 구조 개선 (2026년 6월 11일)

- **Zustand 도입 (`useExcelStore.ts`)**:
  - `page.tsx` 내 산재되어 있던 15개 이상의 `useState` 및 비즈니스 로직(API 호출, 데이터 가공)을 Zustand 스토어로 분리하여 캡슐화했습니다.
  - UI 렌더링 로직과 상태 관리 로직을 명확히 분리하여 가독성과 유지보수성을 향상시켰습니다.
- **컴포넌트 분리 (`UploadPanel.tsx`, `DataGrid.tsx`, `MappingResult.tsx`)**:
  - 파일 업로드 및 정보 표시 로직을 `UploadPanel`로 분리했습니다.
  - 엑셀 데이터를 테이블 형태로 렌더링하고 편집하는 복잡한 UI를 `DataGrid` 컴포넌트로 분리했습니다.
  - 구조 해석 결과를 시각적으로 보여주는 부분을 `MappingResult`로 분리했습니다.
  - 메인 `page.tsx`는 레이아웃 배치만을 담당하도록 간소화되었으며, 각 하위 컴포넌트는 `useExcelStore`를 통해 독립적으로 필요한 상태를 구독합니다.

## 6. D&D 기반 통합 매핑 및 백엔드 영속화 (2026년 6월 12일)

- **D&D 통합 매핑 모델**:
  - **데이터 구조 통합**: 별도의 매핑 객체 없이 `targetColumns` 배열의 각 요소 내에 `frontColumn`(사용자 선택 엑셀 컬럼명)을 포함하는 통합 모델로 리팩토링하여 프론트-백엔드 간 통신을 단순화했습니다.
  - **Drag & Drop UI (`@dnd-kit/core`)**: 시스템 컬럼 배지를 엑셀 헤더 셀로 드래그하여 직관적으로 매핑할 수 있는 UI를 구현했습니다. `DragOverlay`를 적용하여 드래그 중 시각적 피드백을 강화했습니다.
  - **조건부 상호작용**: '헤더' 행이 선택된 상태에서만 매핑이 활성화되도록 제어하여 조작 실수를 방지했습니다.
  - **최종 확정 시각화**: '수정 완료' 버튼 클릭 시 매핑이 확정되며, 배지 형태의 UI가 사라지고 각 셀 우측 상단에 초록색 체크(`✓`) 아이콘이 표시됩니다.

- **백엔드 템플릿 영속화 (MyBatis & MySQL)**:
  - **템플릿 테이블 설계**: `excel_mapping_templates` 테이블을 통해 사용자가 설정한 매핑 규칙(JSON)과 엑셀 구조 정보를 저장합니다.
  - **자동 매핑 (Auto-Mapping)**: 파일 업로드 시 `targetSysType`(업무 구분)을 기준으로 기존에 저장된 템플릿을 조회합니다. 일치하는 정보가 있으면 `frontColumn` 값을 채워서 프론트엔드에 전달하여 자동으로 매핑이 완료된 상태를 만들어줍니다.
  - **파일명 기반 관리**: 템플릿 명칭을 실제 업로드된 파일명으로 저장하여 사용자가 나중에 어떤 양식의 매핑인지 쉽게 인지할 수 있도록 했습니다.

- **자동 매핑 로직 강화 및 이슈 해결 (2026년 6월 12일)**:
  - **프론트엔드 자동 감지**: 백엔드에서 이전에 저장된 매핑 정보(`targetColumns` 내의 `frontColumn`)를 내려주면, 프론트엔드(`useExcelStore.ts`)에서 업로드된 엑셀 데이터의 상위 20행을 스캔하여 해당 컬럼들이 위치한 헤더 행(Row)을 자동으로 찾아냅니다. 찾은 즉시 `isMappingConfirmed`를 `true`로 설정하여 사용자의 별도 조작 없이 체크 아이콘(✅)이 표시되도록 구현했습니다.
  - **문자열 공백 처리**: 엑셀 데이터와 매핑 데이터 간의 공백 차이로 인해 매핑이 실패하는 문제를 방지하기 위해, 양측 문자열을 비교할 때 `.trim()`을 적용하여 안정성을 높였습니다.
  - **Payload 스펙 유지 (`userMapping`)**: 통합된 `targetColumns`만으로도 매핑 정보는 충분하지만, 백엔드 API가 매핑 템플릿 정보를 DB에 Insert/Update 할 때 `userMapping` 필드를 필수적으로 요구하는 스펙으로 되어 있어 이를 Payload에 유지했습니다. (해당 필드가 누락될 경우 DB 저장이 정상적으로 이루어지지 않아, 재업로드 시 매핑 정보가 초기화되는 이슈를 확인하고 조치함). 추후 백엔드 API 스펙 협의를 통해 중복된 `userMapping` 필드를 제거할 수 있습니다.

- **주요 API**:
  - `POST /api/common/upload-excel`: 파일 업로드 및 데이터 조회 + 저장된 템플릿(매핑 규칙) 자동 반환.
  - `POST /api/common/save-excel-data-and-template`: 수정된 엑셀 행 데이터와 새로 생성/수정된 매핑 템플릿을 일괄 저장.

## 7. 다중 헤더 대응 및 워크플로우 최적화 (2026년 6월 13일)

- **다중 헤더 자동 감지 개선**:
  - **문제**: 헤더 정보가 여러 행(Row)에 걸쳐 있는 경우(예: 장비_간편서식.xlsx) 기존 단일 행 스캔 방식으로는 매핑 체크 아이콘(✅)이 표시되지 않는 문제 확인.
  - **해결**: 확정된 헤더 영역(`headerBaseRow` ~ `headerHeight`) 전체를 스캔하여 컬럼 위치(`excelColIndex`)를 정확히 찾아내도록 `useExcelStore.ts`의 자동 매핑 로직을 고도화함.
- **사용자 워크플로우 통합 (자동 구조 해석)**:
  - '구조 해석' 버튼과 '수정 완료' 버튼의 기능을 통합. 사용자가 영역 선택 후 '수정 완료' 클릭 시 내부적으로 `handleConfirmMapping`을 선행 호출하여 최신 구조 해석 데이터를 백엔드(`analyze-excel-structure`)로 자동 전송하도록 개선.
- **영역별 높이(Height) 상태 관리**:
  - `headerHeight`, `recordHeight`(데이터 영역), `etcHeight` 변수를 스토어 상태에 추가하여, 각 영역의 물리적 범위를 명확히 추적하고 구조 해석 결과에 포함하여 백엔드와 동기화함.

## 8. 매핑 상태 관리 및 UI 토글 기능 (2026년 6월 13일)

- **체크 아이콘(✅) 표시 조건 명확화**:
  - 체크 아이콘은 단순히 `frontColumn` 존재 여부만으로 표시되는 것이 아니라, **(1) 매핑 확정 상태(`isMappingConfirmed: true`)**, **(2) 선택된 헤더 영역**, **(3) 셀 텍스트와 `frontColumn` 명칭의 일치**, **(4) 매핑된 열 인덱스 일치**라는 4가지 조건이 충족될 때 렌더링됩니다.
  - 이를 통해 자동 매핑이나 사용자 수동 매핑 결과가 실제 엑셀 시트의 어느 위치에 적용되었는지 시각적으로 명확히 피드백합니다.

- **매핑 정보 수정 모드 (Toggle)**:
  - **요구사항**: 매핑 완료 후 체크 아이콘 상태에서도 다시 컬럼 뱃지(Badge) 형태로 되돌려 수정한 후 다시 확정할 수 있는 기능 추가.
  - **구현**: 
    - `useExcelStore`에 `setIsMappingConfirmed` 액션을 추가하여 매핑 상태를 수동으로 제어할 수 있도록 개선했습니다.
    - `DataGrid` 툴바 상단에 **'매핑 정보 수정 모드'** 체크박스를 도입했습니다.
    - 체크박스 활성화 시 `isMappingConfirmed`를 `false`로 전환하여, 체크 아이콘 대신 다시 드래그 가능한 컬럼 뱃지가 나타나도록 처리했습니다.
    - 수정 후 다시 '수정 완료'를 누르거나 체크박스를 해제하면 매핑 정보가 확정(체크 아이콘 표시)됩니다.

## 9. 영역 자동화 및 다단 구조 영속화 (2026년 6월 15일)

- **영역 자동 추정 및 시각화**:
  - `useExcelStore.ts`: 헤더 감지 시 바로 다음 행부터는 **데이터(Sample) 영역**으로, 이전 행들은 **기타(Etc) 영역**으로 자동 추정하여 초기 상태를 잡아주도록 개선했습니다.
  - `DataGrid.tsx`: 선택된 단일 행뿐만 아니라 **영역 전체(데이터 시작행 이후, 헤더 이전 등)**에 배경색(초록, 파랑)을 입혀 시각적으로 영역 구분이 명확하도록 수정했습니다.

- **다단(Multi-line) 구조 저장 및 복원**:
  - **데이터/기타 영역 끝 행 저장**: 기존 `dataStartRow` 외에 `dataEndRow`, `etcStartRow`, `etcEndRow` 정보를 `headerStructure`에 추가하여 **2줄 이상의 복잡한 영역 정보**를 DB에 영속화할 수 있도록 했습니다.
  - **정밀 복원**: 파일 업로드 시 백엔드에서 전달받은 `headerStructure`를 우선 사용하여, 사용자가 이전에 설정했던 다단 영역 구조를 한 치의 오차 없이 복원합니다.

## 10. 데이터 검증 및 추출 최적화 (2026년 6월 15일)

- **매핑 위치 기반 정밀 검증**:
  - **검증 범위 축소**: 데이터 영역 전체를 검사하는 대신, 사용자가 **실제로 뱃지를 매핑한 칸(열 + 상대적 행 위치)**만 타겟팅하여 검증하도록 최적화했습니다.
  - **필수 값 체크 강화**: 매핑된 칸이 비어있고(`''`) 해당 컬럼이 필수(`required`)인 경우 에러(적색 테두리)로 표시하여 누락 데이터 확인을 용이하게 했습니다.
  - **데이터 경계 자동 계산 (`lastContentRowIndex`)**: 매핑된 열들의 데이터 밀도를 분석하여 **실제 데이터가 있는 마지막 행**을 계산합니다. 이를 통해 하단의 무의미한 빈 줄들로 인해 발생하는 검증 에러를 원천 차단했습니다.

- **DB 저장을 위한 데이터 가공 (`mappedData`)**:
  - `handleExtractModifiedData`: 매핑된 규칙에 따라 엑셀 레코드를 `{ frontColumn, backColumn, value }` 형태의 2차원 배열로 변환하는 기능을 구현했습니다.
  - **다단 추출 대응**: 각 레코드를 `recordHeight` 단위로 읽으며, 기억해둔 **상대 행 위치(`relativeRowIndex`)**의 값을 정확히 추출합니다. (예: 1단 무시, 2단에 있는 금액만 추출 등)
  - **빈 값 필터링**: `value`가 빈 문자열인 필드는 전송 목록에서 자동으로 제외하여 서버 부하를 줄였습니다.

## 11. 백엔드 아키텍처 리팩토링 (2026년 6월 15일)

- **DTO 그룹화 및 파일 슬리밍**:
  - **이너 클래스 패턴**: `ExcelApiDto.java` 컨테이너 클래스를 생성하여, 흩어져 있던 14개의 개별 DTO 파일들을 `static inner class`로 통합 관리하도록 리팩토링했습니다.
  - 이를 통해 `dto` 디렉토리를 획기적으로 간소화하고, 관련 있는 요청/응답 객체 간의 응집도를 높였습니다.

- **템플릿 타입 관리 고도화**:
  - **다중 파일명 지원**: `ExcelTemplateType` Enum이 하나의 타입(예: SIMPLE)에 대해 여러 개의 허용 파일명 리스트(`List<String>`)를 가질 수 있도록 확장했습니다.
  - **JSON 포맷팅**: `@JsonFormat(shape = JsonFormat.Shape.OBJECT)`를 적용하여 Enum 정보를 별도 DTO 변환 없이 API 응답으로 직접 전달할 수 있도록 개선했습니다.
  - **메타데이터 조회 API**: `GET /api/common/excel-templates`를 통해 시스템에 정의된 템플릿과 컬럼 정보를 프론트엔드에 동적으로 제공합니다.

## 12. DataGrid 고도화 및 세트 단위 검증 로직 (2026년 6월 17일)

- **그리드 레이아웃 유지 및 빈 행 상호작용**:
  - **최소 행수 보장**: 데이터 개수와 상관없이 항상 **최소 20개의 행**을 렌더링하도록 패딩 로직을 구현하여 일관된 UI 레이아웃을 유지합니다.
  - **빈 행 편집 활성화**: 패딩된 빈 행들도 hover 효과, 포인터 커서, 직접 타이핑을 모두 지원합니다. 빈 행 편집 시 스토어에서 자동으로 데이터를 초기화하고 행을 확장합니다.

- **멀티 행(2단/3단) 레코드 구조화**:
  - **시각적 그룹화 (Divider)**: `recordHeight`가 2 이상인 경우, 각 레코드가 끝나는 행 하단에 **두꺼운 구분선(`border-b-4`)**을 추가하여 물리적으로 묶여 있음을 명시합니다.
  - **세트 단위 에러 하이라이트**: 레코드 묶음 내부의 단 하나의 셀이라도 검증 오류(필수값 누락, 타입 불일치)가 발생하면, **해당 레코드 세트 전체(2행 또는 3행 전체)에 적색 바탕(`bg-red-50`)**을 적용하여 "세트 단위의 오류"임을 직관적으로 알려줍니다.

- **검증 로직 정밀화**:
  - **검증 시작점 최적화**: 사용자가 구조 파악을 위해 선택한 '샘플(초록색) 행'은 검증에서 제외하고, **그 바로 다음 행부터** 실제 검증이 시작되도록 로직을 다듬었습니다.
  - **동적 검증 범위 확장**: 사용자가 아래 빈 행에 데이터를 추가하면 `lastContentRowIndex`가 자동으로 갱신되며, 레코드 세트가 완성되는 지점까지 검증 범위가 동적으로 확장됩니다.
  - **실시간 필수값 피드백**: 별도의 '데이터 검증' 버튼 클릭 전이라도, 매핑이 확인된 상태라면 즉시 필수값 누락 여부를 체크하여 사용자에게 알립니다.

## 13. 데이터 검증 자동화 및 숫자 포맷 대응 (2026년 6월 18일)

- **검증 프로세스 완전 자동화**:
  - **불필요한 버튼 제거**: 기존 '데이터 검증' 버튼을 제거하고, 파일 업로드 시점부터 실시간으로 검증이 수행되도록 변경했습니다.
  - **트리거 포인트 확대**: 초기 로드 시 매핑 정보가 있으면 즉시 검증, 배지 드래그로 매핑 변경 시 즉시 재검증, 사용자가 셀 값을 수정하는 즉시 실시간 검증이 수행되도록 구현했습니다.
  - **수정 완료 버튼 연동**: 실시간 검증 결과(`hasTypeError`)가 즉각 반영되어, 모든 데이터가 유효할 때만 '수정 완료' 버튼이 활성화됩니다.

- **숫자 형식 유연성 확보**:
  - **천 단위 구분 기호(,) 허용**: "3,000"이나 "123,000"과 같이 콤마가 포함된 문자열도 숫자(`number`) 타입으로 정상 인식하도록 `getDataTypeAndPattern` 및 `isValidType` 로직을 개선했습니다. 콤마를 제거한 후 정규식 검사를 수행하여 데이터 타입 오판을 방지합니다.

- **매핑 상태 관리 고도화**:
  - **자동 수정 모드 진입**: 매핑된 컬럼 중 하나라도 해제되거나 변경되면 `isMappingConfirmed`가 자동으로 `false`가 되어, 사용자가 즉시 수정 모드 UI(컬럼 배지 등)를 볼 수 있도록 했습니다.
  - **매핑 미완료 시 확정 방지**: 모든 필수 컬럼이 매칭되지 않은 상태에서는 매핑 확정 상태로 전환되지 않도록 안전장치를 추가했습니다.

## 14. 향후 범용 업로드 엔진 고도화 계획 (Future Tasks)

- **구조/명칭 무관성 원칙**:
  - 엑셀 파일의 이름이나 시트 내부의 구체적인 레이아웃(데이터가 몇 번째 행/열에 있는지 등)에 구애받지 않고, 시스템이 요구하는 **필수 컬럼명과 데이터 타입(정규식)**만 일치한다면 매핑 과정을 거쳐 즉시 DB에 저장 가능한 범용 업로드 엔진을 지향합니다.
  - **적용 계획**: 현재는 프로토타입 UI 및 핵심 로직 검증 단계이며, 향후 최종 화면 구성(디자인)이 완료되는 시점에 이 범용성 로직을 전면 적용하여 어떤 형태의 엑셀 양식도 수용할 수 있도록 고도화할 예정입니다.

## 15. Store 상태 관리 보완 및 타입 누락 수정 (2026년 6월 19일)

- **`TargetColumn` 인터페이스 수정**:
  - 백엔드에서 전달되는 `dataType` 및 `regex` 필드가 프론트엔드 상태 관리에 유실되지 않도록 `useExcelStore.ts`의 `TargetColumn` 인터페이스에 해당 필드를 명시적으로 추가했습니다.
- **백엔드 매핑 데이터 초기화 고도화**:
  - `fetchChunk` 호출 시 백엔드로부터 응답받은 `targetColumns` 데이터를 프론트엔드 상태에 초기화할 때, `dataType`과 `regex`가 함께 보존되도록 매핑 로직(`...col`과 더불어 명시적인 null 병합 연산자 `??` 사용)을 보강했습니다.

## 16. 백엔드 스펙 변화 대응 및 다차원 수동 매핑 검증 고도화 (2026년 6월 20일)

- **`backColumn` 필드 제거 및 `name` 단일화**:
  - 백엔드 응답 스펙 변화(`backColumn` 필드 누락)에 맞춰 프론트엔드 상태 및 매핑 식별 로직을 `name` 기준으로 통합했습니다.
  - 백엔드로 매핑 템플릿과 정제 데이터를 송신할 때도 `back-column` 키의 값에 `col.name`을 할당해 API 호환성을 맞추었습니다.
- **`dataType` 기반 배지 스타일 다변화 및 필수 필드 가시화**:
  - `number`(초록), `phone`(노란), `biz-number`(파란), `string`(회색) 등 데이터 타입별로 뱃지 색상을 차별화하여 한눈에 식별되도록 개선했습니다.
  - 필수 컬럼(`required`)의 경우 붉은색 테두리(`ring`)와 별표(`*`) 표식을 추가하여 시각적 직관성을 향상했습니다.
- **다단(Multi-row) 매핑 데이터 구조 연동 개선**:
  - 헤더 영역 내 여러 행에 걸쳐 매핑된 복수의 헤더와 데이터 타입을 결합할 때, 헤더와 데이터의 상대적 위치(`relativeRow`)를 1:1로 정확하게 짝지어 병합하도록 `mergeHeaderAndType` 함수를 수정했습니다.
- **데이터 단수에 따른 유연한 수동 드롭 검증 및 표시**:
  - 헤더와 데이터 영역의 행 개수 불일치 상황(예: 2단 헤더 & 1단 데이터)에 대응하여, 사용자가 드롭한 헤더 셀의 상대 위치(`relativeRow`)가 있더라도 실제 데이터 단수(`recordHeight === 1 ? 0 : relativeRow`)에 따라 올바른 행의 데이터 타입을 검증하고 표시하도록 개선했습니다.
  - 드래그 앤 드롭 시 시스템 컬럼과 드롭 존의 데이터 타입이 불일치하면 매핑되지 않고 경고 창이 뜬 후 튕겨 나가게 처리하여 오매핑을 방지했습니다.
  - 공백이나 쉼표가 혼재된 형태의 숫자(`123123, 010111000`)도 타입 감지기에서 `number`로 올바르게 식별하도록 공백 제거 및 쉼표 필터(`replace(/[\s,]/g, "")`)를 보강했습니다.

## 17. 엑셀 디폴트 초기화 설정 및 백엔드 스펙 연동 검증 고도화 (2026년 6월 21일)

- **기본 레이아웃 초기화 설정**:
  - 엑셀 업로드 시 기존에 저장된 매핑 템플릿이나 자동 감지된 헤더 행이 존재하지 않는 경우, 첫 번째 행(Index 0)은 헤더 영역, 두 번째 행(Index 1)은 데이터 영역으로 기본 초기화되도록 스토어 초기 로드 로직을 변경했습니다.
- **선택 모드 전환 시 기존 영역 상태 리셋**:
  - 헤더, 데이터, 기타 영역 선택 모드 전환 버튼을 누를 때, 기존에 선택되었던 각 영역의 세부 정보(선택 행 인덱스, BaseRow, Height 등)를 완전히 초기화(리셋)해주는 방식을 `setMode` 액션에 도입하여 사용자 편집 편의성을 대폭 향상했습니다.
- **백엔드 정규식(`regex`) 연동 검증 고도화**:
  - 데이터의 형태만 보고 프론트가 판정한 임시 타입(`fieldSchema`) 기준 검증의 한계를 극복하고, 실제 매핑된 시스템 컬럼의 백엔드 스펙(`dataType` 및 `regex`)을 기준으로 유효성을 검사하도록 검증 로직(`isValidType` 및 셀 에러 바인딩)을 전면 리팩토링했습니다.
  - 이 과정에서 이중/삼중으로 이스케이프되어 수신되는 정규식 문자열(`\\\\`)을 단일 백슬래시(`\`)로 정규화하는 필터링을 탑재하여 하이픈(-) 생략 여부와 관계없이 숫자형 포맷(예: 사업자번호 `1234567890`)이 정밀하게 검증되도록 보정했습니다.
- **하이브리드 드래그 앤 드롭 매치 규칙 적용**:
  - 드롭하려는 엑셀 컬럼 타입과 시스템 컬럼 타입이 불일치하더라도 즉각 차단하지 않고, 엑셀 샘플 데이터 값이 시스템 컬럼의 정규식(`regex`)에 통과하면 매칭을 유연하게 수락하며, 정규식에 일치하지 않을 때만 경고와 함께 튕겨내도록 매핑 가드 로직을 고도화했습니다.
- **헤더 내 타입 렌더링 라벨 제거**:
  - 타입 불일치 허용 규칙이 유연해짐에 따라 노란색 헤더 행 하단에 불필요하게 렌더링되던 데이터 타입 안내 텍스트(예: `(string)`, `(number)`)를 삭제하여 그리드 레이아웃의 시인성을 한층 높였습니다.
