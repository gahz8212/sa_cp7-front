# 엑셀 업로드 및 매핑 기능 구현 메모 (2026-06-04 수정)

## 1. 개요
프론트엔드(Next.js)에서 엑셀 파일을 업로드하고, 백엔드로부터 분석된 데이터를 받아 그리드(Grid) 형태로 표시 및 수정하며, 복잡한 2단 헤더/데이터 구조를 사용자가 직접 매핑(Flattening)하여 템플릿화 할 수 있는 기능을 구현함.

## 2. 주요 기능 및 로직
- **인터랙티브 매핑**: 
    - 사용자가 헤더 행과 데이터 행을 모드별로 선택 가능.
    - 선택된 행은 하이라이트(노란색: 헤더, 녹색: 데이터) 및 적색 테두리로 시각화.
- **구조 분석 알고리즘**:
    - `buildStructure` 함수를 통해 헤더와 데이터의 계층 구조를 독립적으로 해석.
- **백엔드 연동 개선 (2026-06-04)**:
    - 백엔드에서 자동 탐색한 데이터 구조를 완벽히 수용.
    - 전체 엑셀 행을 그리드에 노출하고, 엑셀의 실제 행 번호를 좌측에 표시.
    - 자동 하이라이트 기능은 제외하고, 사용자의 수동 선택을 기준으로 헤더 확정.

## 3. 이슈 및 해결 기록
- **데이터 컬럼 제한(15개):** 해결 (백엔드 전체 행 파싱 로직 수정).
- **`.xls` 파일 업로드 이슈:** 해결 (백엔드 `WorkbookFactory` 도입 및 라이브러리 의존성 추가).

## 4. 향후 작업 계획 (2026-06-05 예정)
- 템플릿 파일 생성 및 관리.
- 템플릿 기반 데이터 검증 로직 구현.
- `file_key`를 원본 파일명 기반으로 할당하는 로직 적용.
