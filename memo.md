# 엑셀 업로드 및 매핑 기능 구현 메모 (2026-06-04 수정)

## 1. 개요
프론트엔드(Next.js)에서 엑셀 파일을 업로드하고, 백엔드로부터 분석된 데이터를 받아 그리드(Grid) 형태로 표시 및 수정하며, 복잡한 2단 헤더/데이터 구조를 사용자가 직접 매핑(Flattening)하여 템플릿화 할 수 있는 기능을 구현함.

## 2. 주요 기능 및 로직
- **인터랙티브 매핑**: 
    - 사용자가 헤더 행과 데이터 행을 모드별로 선택 가능.
    - 선택된 행은 하이라이트(노란색: 헤더, 녹색: 데이터) 및 적색 테두리로 시각화.
- **구조 분석 알고리즘**:
    - `buildStructure` 함수를 통해 헤더와 데이터의 계층 구조를 독립적으로 해석.
- **백엔드 연동 개선 (2026-06-04)**:
    - 백엔드에서 자동 탐색한 데이터 구조를 완벽히 수용.
    - 전체 엑셀 행을 그리드에 노출하고, 엑셀의 실제 행 번호를 좌측에 표시.
    - 자동 하이라이트 기능은 제외하고, 사용자의 수동 선택을 기준으로 헤더 확정.

## 4. 이슈 및 해결 기록
- **데이터 컬럼 제한(15개):** 해결 (백엔드 전체 행 파싱 로직 수정).
- **`.xls` 파일 업로드 이슈:** 해결 (백엔드 `WorkbookFactory` 도입 및 라이브러리 의존성 추가).
- **수정 데이터 추출(Diff 체크):** 
    - `originalData` 상태를 활용하여 행 단위 `JSON.stringify` 비교 로직 구현.
    - 변경된 행만 추출하여 `{ rowIndex, original, modified }` 구조의 JSON 객체 생성 성공.
- **백엔드(Spring Boot) 연동 에러 해결:**
    - **400 에러**: `@RequestBody`와 DTO 필드명(`modifiedRows`) 일치 확인으로 해결.
    - **403 에러**: `@LoginUser` 리졸버의 인증 요구 사항 및 프론트엔드 `apiClient` 사용 필요성 확인.
- **페이징 데이터 미표시 이슈 (2026-06-07):**
    - **현상**: 2페이지 이동 시 데이터가 보이지 않음.
    - **원인**: 백엔드 API가 프론트엔드에서 요청한 `size: 1000` 파라미터를 무시하고 20개 단위로만 데이터를 반환하거나, 클라이언트에서 전체 데이터를 누적하지 못하는 로직 불일치 발생.
    - **해결 방향**: 백엔드 API 응답 페이징 로직 수정(요청 `size`만큼 반환) 또는 프론트엔드 매 페이지 이동 시마다 데이터 요청 방식으로 전환.

## 6. 기능 분석 결과 (2026-06-07)
### 1) 데이터 로드 및 페이징 (Pagination)
- 현재 코드(`apps/admin/app/test/excel-upload/page.tsx`)에는 인피니트 스크롤(스크롤 시 자동 로딩)이 구현되어 있지 않음.
- **방식**: 사용자가 수동으로 '다음' 버튼을 클릭할 때, 현재 페이지와 청크 단위를 계산하여 부족한 데이터를 백엔드에서 가져오는 'Chunked Loading' 방식임.

### 2) '수정 완료' 버튼 로직
- 클릭 시 `handleExtractModifiedData` 함수가 실행됨.
- 로직: `allData`(현재값)와 `allOriginalData`(초기값)를 행 단위로 비교(JSON.stringify 활용)하여 변경된 행만 추출.
- 결과: 변경된 행 객체(`{ rowIndex, original, modified }`)를 `modifiedRows`라는 이름의 JSON 데이터로 백엔드의 `/api/common/save-excel-changes` API에 POST 전송.

### 3) '구조 해석' 버튼 로직
- **특징**: 백엔드와 통신하지 않는 **완전한 클라이언트(브라우저) 기반 로직**.
- 로직: 사용자가 선택한 행들을 `buildStructure` 함수로 분석하여 구조를 파악하고, 그 결과를 내부 상태(`mappingResult`)에 저장하여 화면에 즉시 표시함.
